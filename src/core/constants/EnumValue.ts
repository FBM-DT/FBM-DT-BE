export enum WORKTYPE {
  DAILY = 'daily',
  EVENT = 'event',
}

export enum ACCOUNT_ROLE {
  ADM = 'admin',
  SUPERVISOR = 'supervisor',
  USER = 'user',
}

export enum SEARCH_TYPE {
  AND = 'and',
  OR = 'or',
}
